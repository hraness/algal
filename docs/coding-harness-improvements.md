# ALGAL coding-harness improvement ledger

This ledger distinguishes implemented example-level fixes from proposed ALGAL
work. A promising direction or a missing convenience API is not evidence that a
new core feature is required. Live results must be attached before closing items
whose acceptance criteria depend on live inference or benchmark performance.

| ID | Status | Observation / evidence | Current action and acceptance criterion |
| --- | --- | --- | --- |
| CH-01 | Implemented in pilot; core change unnecessary | `src/run.ts` already owns `agent` cell tool loops; `examples/coding-harness/harness.ts` supplies one model call and one terminal effect at a time. | Compare deterministic baseline/ALGAL traces and verify completed ALGAL receipts. Do not wrap an entire external coding agent and call that an ALGAL-owned loop. |
| CH-02 | Implemented in pilot | `examples/coding-harness/protocol.ts` admits a bounded data-only policy and hashes canonical values. | Reject unknown fields and code-bearing proposals; bind model, runtime, prompt version, and limits in the experiment report separately. The policy digest alone is not an experiment identity. |
| CH-03 | Live pipeline qualified; no solving benefit established | The [completed pilot](coding-harness-pilot-results.md) ran 12 valid trials, all failures, and verified all eight ALGAL receipts. Development selection was frozen before holdout. | Retain every candidate and proposal cost. Treat this as integration evidence; four tasks and one repeat do not establish performance or equivalence. |
| CH-04 | Host adapter qualified on the recorded backend | `xcb.ts` supplied individual model calls without delegating the coding loop. The pilot and smokes made 47 calls through the qualified existing subscription backend, with no paid API fallback. | Keep exact backend identity and qualification checks. Token/subscription-cost attribution remains unknown; zero incremental paid API spend is not zero total cost. |
| CH-05 | Open; measure before extending core | Test/recovery labels change instructions, not enforced transitions. All six development traces edited before inspecting despite the inspection instruction. | Encode a narrow inspect/test/repair structure using existing tool/repeat/guard primitives, then compare behavior. Consider core changes only for a demonstrated missing semantic operation. |
| CH-06 | Model proposal, evaluation, and freeze live-qualified | One generated proposal changed context retention to first-plus-recent, capped at four messages. It scored 0/2 on development, tying the parent; the declared digest tie-break retained the original policy. Its proposal call and both trials are included in accounting. | Next compare equal-budget conventional search and richer development evidence. This is one bounded policy-search step, not sustained autonomous evolution; no improved policy was promoted. |
| CH-07 | Open; application responsibility | Completed ALGAL receipts are replayable, but ordinary host terminal writes are not automatically crash-safe or idempotent. See `docs/coding-operations.md` and `src/process.ts`. | Use isolated task environments and surface uncertain termination without automatic retry. A durable live harness needs a qualified effect adapter and reconciliation evidence before claiming safe restart. |
| CH-08 | Deferred pending a useful fixed-policy result | Code, selected context, evidence, and stored values are accessible through separate existing APIs; host callback implementations remain opaque. See `src/store.ts`, `src/registry.ts`, and `spec/v1/organism.md`. | Record candidate ancestry and policy/evidence digests at the application level. Add a general introspection primitive only when a concrete evolving component cannot obtain the information it needs through current APIs. |
| CH-09 | Memory spike implemented; evaluation pending | The first pilot has no cross-task persistent memory treatment. | Compare the same frozen policy with/without memory on a predeclared task sequence, then test a memory-schema migration. Preserve source experience and provenance. |
| CH-10 | Open; report limitation | Task-ID disjointness does not establish semantic independence or absence of benchmark contamination. | Review task provenance, pin revisions, prevent hidden grader access, and expand to enough repeats/tasks before drawing performance conclusions. |
| CH-11 | Open; provider limitation | The configured XCB selector is `claude/sonnet/low`; executable and qualification evidence digests are available, but immutable upstream model weights are not. | Record the selector and available qualification identity per episode. Do not claim exact model-version pinning; if the backend changes during comparisons, record it and rerun affected comparisons as needed. |
| CH-12 | Fixed in pilot; core change unnecessary | Integration review found that escaped full traces could hit ALGAL's context cap earlier than the baseline, while an extra serialization bound incorrectly rejected valid multiline terminal output. | Both engines now enforce the same 262,144-byte canonical unprojected context bound. Regression tests in `harness.test.ts` cover equal stopping points under full/recent policies and acceptance of bounded multiline output. |
| CH-13 | Fixed in pilot integration | Harbor forwards agent `env` fields into sandbox execution and mounts the normal agent-log directory into the task. Those defaults exposed host routing and could let a task tamper with controller evidence. | `pilot.py` now passes inference routing only through the parent process environment; the adapter keeps controller evidence in the unmounted `algal-host` sibling directory. Keep these boundaries in integration tests. |
| CH-14 | Fixed in pilot integration | Startup capability queries could ignore cancellation; terminal output was initially buffered without a bound by Harbor before trimming. | XCB startup now has a cancellation deadline and joins its child. The controller joins outstanding terminal replies. A sandbox-side Perl helper drains excess output, retains bounded bytes, and settles command processes on timeout. Adapter regressions cover noisy output, cancellation, and private host evidence. |
| CH-15 | Fixed adapter dependency; live qualification complete | The first `regex-log` smoke completed one model call but its Python capture wrapper exited 127: the pinned Ubuntu image has no Python interpreter. The model command never executed, and the trial remains invalid. | `perl-core-v2` passed noisy-output/timeout qualification and a valid scored smoke (reward 0). Final `perl-core-v3` passed background-settlement qualification and all 12 matrix trials without capture failures. Neither required modifying benchmark images. |
| CH-16 | Fixed in experiment runner | Example-file hashes alone did not bind the imported ALGAL runtime; immutable image preflight originally lived only in local environment scripts. | Bind actual runtime source/WASM bytes in settings. Before inference, require each cached image's exact benchmark digest and platform; retain Docker version/image evidence per launch. The runner never pulls or rebuilds task images. |
| CH-17 | Fixed; persistent background jobs unsupported | Review of the first Perl helper found that a successful shell could leave ordinary background descendants running after its pipes closed. | `perl-core-v3` terminates remaining command groups on normal completion too and requires confirmed disappearance before a clean reply. A delayed-writer regression verifies the child is gone and cannot write after return. Unproven settlement remains invalid; general persistent jobs need a different explicit lifetime contract. |
| CH-18 | Open; establish baseline competence | The four-call pilot produced zero successes. All three development polyglot trials exhausted their budgets; missing-interpreter discovery consumed calls, while regex runs finished without a successful behavioral check. | Separate interface fixes from a call-budget ablation. Establish a task/budget combination with measurable fixed-loop competence before interpreting policy-search differences. |
| CH-19 | Mechanism implemented; fixed-prompt comparison pending live evidence | One development run claimed it lacked tool access after executing three valid terminal actions. XCB exposes no native tools; returning terminal JSON is the host execution request. Its causal contribution is unproven. | The proxy explanation and remaining-attempt budget now appear in every model response (`harness.test.ts`). Compare against the earlier prompt with model/tasks/four-call budget fixed before attributing benefit. Do not infer transport failure from the model's claim. |
| CH-20 | Fixed in memory spike; deterministic parity verified | The proposed four-message projection produced `[user, tool, assistant, tool]`, retaining a result while dropping its originating command. Full-context runs also failed, so this is not a demonstrated cause of the score. | Preserve complete action/result groups under the byte/message bounds in both engines, retain the full source trace, and evaluate separately before claiming a benefit. |

| CH-21 | Implemented in opt-in example | Native positive Datalog already exists; the harness lacked source admission, dependency scope, and an asynchronous bridge. | Validate immutable observation bytes, closed probe procedures, prerequisite proofs, and replay without effects before any performance claim. See [memory spike](coding-harness-memory-spike.md). |
| CH-22 | Implemented in memory spike; deterministically covered | An unrelated probe could revive prior observations after a terminal mutation, and a restart could forget invalidations. | `memory.ts` persists `invalidatedRefs` per source across same-task restarts and task renames; fresh probes admit only their own observations. `memory.test.ts` covers both boundaries under the native gate. |
| CH-23 | Experiment design limitation | Episodic/logical comparison changes retrieval and representation as well as derivation. | If the package helps, compare identical structured facts with and without derived answers before attributing benefit to Datalog. |

## Update rules

For each observed issue, retain the failing task/run ID, exact configuration,
symptom, and smallest reproducer. Link a focused test or concrete live artifact
when moving an item to implemented/qualified. Keep live qualification distinct
from deterministic implementation checks. Do not attach secrets, hidden solutions,
or private subscription/account state to this public ledger.

Small fixes that preserve the existing contract belong in this pilot or an
ordinary focused patch. Coordinate any public-runtime or wire-contract changes
with the parallel ALGAL owner before editing shared core files.
