# Upstream integration after checkpoint 5e334ed

The checked checkpoint `5e334ed` is published in [PR 70](https://github.com/hraness/algal/pull/70). The pending merge includes upstream `1737efe77375ccb35f3ed68fb465473019d2e573`, which adds browser storage and execution, portable hashing and text encoding, and native in-process host executors. Earlier saved results identify their original source and remain historical evidence.

Independent source review found two additional runtime defects. A native executor error without a saved effect could become an ordinary cell failure, producing a run that replay could not reproduce. The runtime now propagates that host failure. Configuration changes detected after a callback remain uncertain, unfinished journal intents remain blocked, and suspension handles are checked before settlement. Recorded executor errors still support ordinary failure edges.

Browser raw recovery used `Array.map`, which skipped sparse positions and serialized them as null. A million sparse positions also escaped the node limit. Recovery now requires dense own positions and counts compact JSON bytes before allocating an escaped representation. It rejects negative zero because JSON serialization would change it to zero. The import conflict in `application-contract.ts` preserves the portable Store interface and the earlier typed error and digest checks.

## Checks on the merged working source

The complete source inventory has 924 bindings, digest `cecc01647a3fdfa5451af94f10f37035d7e6c1826c9f8752e41c9da1fc075772`. Git still identified parent checkpoint `5e334ed` during these runs; the file inventory identifies the pending merge and repairs. A clean Git label alone would not identify these source bytes.

- `bun run check` passed 1434 tests and 15868 assertions, with 20 explicit skips. Typechecks, lint, seven executable diagrams and 55 generated pages passed. [Raw repository gate logs and build metadata](../../verify/results/9b1185728a8cefaa22a084e889b54bdfea3106c24b5e912d691b258bc1997d09/integration-gates.json). Earlier failures are kept separately: a typed-array constructor mismatch was repaired; a sandbox-denied loopback run and a concurrent-filesystem timeout are not counted as passing evidence. The final run used permitted local listeners after native filesystem tests completed, with unchanged test deadlines.
- Native formatting, Clippy, workspace tests and debug/release builds passed with pinned Rust 1.97.1. The ordinary test harness passed 388 tests; three owned child fixtures are intentionally ignored by that generic harness and invoked by dedicated checks.
- All 20 runtime comparison commands passed. These include 62 examples, 250 application steps, 58 process steps, 15 store steps, 65 CLI cases, 30 schema comparisons, two example families, native memory fixtures, five recovery/verification demonstrations, and three local inference formats with six offline verifications. [Complete raw comparison archive](../../verify/results/e18926856bcce7b99253b7c9b5cabcd4e35c31c61a73b91d8bb6152655e238be/integration-parity.json).
- Chromium 153.0.8010.12 passed 15 checks covering persistent storage, recovery, reloads, cross-tab locking, import/export and offline worker startup. Independent review rehashed all 924 source files, 353 browser records and 205 generated-site records. [Browser records and screenshots](../../verify/results/9ec9308e9df7c821f79c7463536bdc7329a47d22fa51022c331b921d5f1b2cd8/browser-qualification.json). The interruption fixture injects an error before head publication; it is not a process-crash or physical-power-loss test. No GPU/model inference was requested, and zero remote requests means observed application traffic. The installed Playwright driver and operating system remain trusted.
- A separate Node/V8 process agreed with Bun's portable helpers on 298 cases, 7,471,912 encoded bytes, 4,768 truncations and 1,024 ordering comparisons (7,580 assertions). Cases include SHA padding boundaries and Unicode strings up to 1 MiB. [Raw cross-engine evidence](../../verify/results/e704ab9787fbf0d99d553a3468b4429d327fc4f02c2856eb7f9955e41f39c281/portable-cross-engine.json). Earlier finite probes covered 196,608 UTF-16/surrogate comparisons and 1,024 deterministic hash comparisons. Cryptographic collision resistance remains an assumption.

The 23-suite formal aggregate returned a passing result on the same source digest. It includes 661 TLC calls: 183 finite model profiles, 374 action witnesses and 104 expected counterexamples. Lean checked 564 authored theorems (1,616 module theorems) and seven rejected negative controls; the scheduler compared 206 fixed Bun/native cases. [Independent raw evidence readmission](../../verify/results/37334844a7d400d7e1451e6e61b5b695dc70bb9f4cf6199529859293819ecdef/aggregate-readmission.json) passed for all 23 suites. Repository CI and final delivery checks still apply.

## New boundaries and remaining work

The ledger adds HST-13 for registered native execution, HST-14 for browser storage/recovery, and HST-15 for local model worker lifecycle. It contains 119 obligations, 103 not started and 16 observed, with zero licensed production correctness claims. VAL mappings include `utf8.ts` and `sha256.ts`; storage/package mappings include the portable interfaces. A-BROWSER records origin, transaction, lock, eviction and worker assumptions separately from filesystem persistence.

IndexedDB snapshots limit record counts, but corrupt oversized stored values can allocate memory before recovery validation. Browser heap limits remain open. Registered native callbacks must obey the stated host assumptions; these checks do not establish callback termination, allocation limits or external-effect truth.

Verification identity now includes browser source, root browser tests and all scripts, excluding generated `site/dist`. Lean correspondence also binds the complete governed production inventory. This prevents helper changes from preserving obsolete correspondence evidence; it does not extend the mathematical proof to those helpers.

The expression corpus and generated application histories remain isolated candidates. The corpus integration has independent source review and 27 focused tests; it requires fresh execution after import. Separate scratch campaigns passed four application histories with five minimized mutation controls and seventeen dynamic-spawn fixtures. [Application raw evidence](../../verify/results/0859f79f1516a833239569e7d4bcb840adb817980c52f55b5e41e776a698d9ac/application-history-scratch.json) preserves 105 worker records; random replay directory names do not reconstruct the complete shrink chronology. [Spawn raw evidence](../../verify/results/825623020664b16dd71e1983f066efa216611b0aa500f640dd09d4ac70252843/spawn-scratch.json) preserves 88 supervised commands and independent offline readmission. These archives do not qualify the later registered suites. Constructive Lean neighbor and rational bracket extensions remain reviewed candidates pending integration. The remaining plan phases and implementation refinement remain open.
