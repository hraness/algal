# Algal final integration review — current slice

**Verdict: source integration approved; no new blocking defect found in the reviewed slice.** This is independent review of the current integration, not completion of the 19-phase assurance program. Final repository/formal/parity gates and any resulting repairs remain the integration owner's responsibility. This review does not itself authorize a failing gate or stale evidence to be accepted.

## Identity and method

Reviewed `/Users/bg/Documents/algal`, branch `feat/formal-verification-foundation`, HEAD `f461dd2deb11c11746af38fcb480380b44e2ba29`, with the active no-commit merge of `e588f8bc9b0964e5956f4c62e60c14dfbcef171e`. Source identities for the inspected seams are recorded in `/private/tmp/algal-final-integration-review-sources.json`. There were no unmerged paths at review time. I read AGENTS.md, CONTRIBUTING.md and the assess-change-impact skill; inspected source, tests, contract changes, binding/claim admission and prior independent lane reports. I did not edit repository files, launch tests/builds/models, commit, push or run another owner's command.

The earlier merged-foundation report is composition evidence, not a current-tree gate. Bundle source hashes still match the independently reviewed final handoff. Directory production hashes match the reviewed handoff; later documentation changes accurately narrow allocation claims and the later test cost repair has its own independent approval. Full green native, renderer, Python, Apple-fixture and release-smoke results are integration-owner evidence; they were not independently rerun here.

## Integration conclusions

1. **Application storage extraction preserves custody and failure meaning.** The filesystem facade forwards the diagnostic option to its adapter and preserves its FileStore-facing API. The permanent pending application mutex is selected independently of head state. Existing namespaces also acquire the permanent legacy mutex. Creation delays the namespace until admission/quota publication, and prepared namespaces can resume at capacity. `legacyHeld` is per adapter and per application, set/cleared within the permanent lease callback. The core's outer `publicationAttempted` catch now covers head-write, publication-return and custody-return cuts. It conservatively returns uncertainty for the exact attempted operation, while exact committed replay and pre-publication refusal preserve their original error. Real memory/filesystem boundary tests cover these distinctions. The contract rehashes records from every injected Store, including FileStore subclasses, before parsing normalized data; no class-name trust shortcut survived extraction.

2. **Effect and journal closure compose across the moved modules.** `commandJson` resolves through `io-runtime`, recall shape binding through `semantic-contract`, and the canonical byte function uses TextEncoder. The cache invocation observer is tied to the exact executeEffect function; it distinguishes preflight lookup metadata from the current live invocation. Explicit replay remains separate. Post-result encoding/cache-publication errors and failed live journal completion preserve host uncertainty and original diagnostics. Poisoned native journals escape guest routing; process handling retains the original failure when settlement refuses. Native cache hits retain ordinary completion behavior and replay bypasses live journal publication. Final receipt admission includes the digest field before hashing; journal admission counts the complete host wrapper before durable writes. Existing lane reports accurately limit these guarantees to the modeled/admitted data domain.

3. **Bundle producer and consumers agree on the admitted envelope.** Distinct manifests and values are capped independently; repeated references do not double-charge entries. Incremental bytes/nodes/depth account for the wrapper. Both import paths admit the supplied and manifest-normalized envelope before destination writes. Bun's direct JSON byte counter handles control escaping, valid surrogate pairs and lone-surrogate escape bytes; its parser stops at the count limit before reading the excess value. Public docs preserve partial imports and explicitly deny dependency-completeness/rollback implications. This is a behavior change for previously accepted overbound bundles, intentionally surfaced as BUDGET_EXHAUSTED without raising bounds.

4. **Ingress and directory changes preserve error and residue contracts.** CLI call/unpack use the existing descriptor-based regular-file, capped-read, fatal-UTF-8 reader. Directory counters precede filtering and stay distinct from logical mailbox/module limits. An initially missing optional namespace is empty; invalid namespace or reported iterator failure is an error. Listing records keep bounded reads and per-record error rows. No scan repairs capacity by deleting custody/recovery residue. Production counter changes are not advertised as a bound on Bun's eager opendir/Glob allocation; libc/filesystem behavior also remains an explicit native assumption. Direct digest reads remain available beyond listing capacity.

5. **New regression fixtures exercise the actual boundary.** Storage tests reopen filesystem adapters and inspect retained heads/operations; corrupt Store-subclass tests check rejection before admission/publication. Gateway tests use real FileStore, ProcessSupervisor and journals, with local fetch responses; observer failure, cache reuse, offline replay and late completion after a deadline are represented. The losing Gateway promise is owned and joined after barrier release. Ingress commands use the command supervisor and defer namespace cleanup until successful collection. The directory fixture optimization retains exact physical/logical boundaries and child deadlines; it changes setup/read cost, not the accepted domain. These are regression/conformance examples, not proof of arbitrary provider behavior.

6. **Claim admission remains fail closed.** The ledger contains 116 properties and zero licensed claims; only not-started/observed statuses are present. The production evidence admission adapter still refuses stronger states instead of deriving a source proof from a model/test receipt. Governed inventories include untracked files, extracted companions and current examples; verification inventories separately bind the proof/model/runner inputs. The application protocol inventory now includes core and storage-boundary tests and checks every named file separately. The current docs distinguish historical 564-theorem evidence after its bound specification changed and require fresh aggregate readmission. Phases 06–09 remain in progress; no phase-completion or whole-system correctness claim is warranted by this review.

## Reachable impact and remaining gates

Direct consumers are the portable/filesystem application APIs, effect/cache/journal execution, bundle SDK/CLI, mailbox namespace admission and CLI listings/modules. Indirect consumers include process recovery, replay/offline verification, host policy and application messaging, injected stores and the extracted upstream adapters. Operational changes include more conservative uncertainty/reconciliation and earlier bounded rejection; no migration removes records or rewrites existing user data. Gateway tests qualify composition with deterministic fixtures, not live provider attestation, inference quality or remote exactly-once delivery. No production deployment or live data write was performed in this slice.

At report time the root's required final Bun and all-required formal aggregate were running, and the application worker owned the native-backed/parity jobs. Collect those exact results, readmit saved evidence against the frozen input tree, then perform the repository PR/check/merge workflow. A repair in a reviewed seam requires focused rereview and any affected gate rerun. Historical receipts and earlier failing timeout/DNS/loopback attempts must retain their actual status.

Open obligations remain explicit and do not invalidate this narrower repair: a qualified directory primitive, interrupted-process inner residue, complete corpus/mutation/fuzz/shrink coverage, complete producer/consumer wrapper closure and pre-effect reservation, universal scheduler/source refinement, full numeric rounding/shortest-spelling/production codec proofs, and later assurance/hosted phases. Keep Phases 06–09 open until their actual acceptance criteria are satisfied.
