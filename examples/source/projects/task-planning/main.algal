import plan_task from "./plan_task.algal"

program task_planning(tasks: json, weights: json) -> json {
  budget { max_agent_calls: 0, max_steps: 1024, max_depth: 4 }

  return each plan_task over task in tasks using { weights: weights } max_items 16
}
