import triage_ticket from "./triage_ticket.algal"

program support_queue(tickets: json, weights: json, window: json) -> json {
  budget { max_agent_calls: 0, max_steps: 512, max_depth: 4 }

  return each triage_ticket over ticket in tickets
    using { weights: weights, window: window } max_items 8
}
