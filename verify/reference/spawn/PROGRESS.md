# Finite progress argument

The copied oracle keeps its existing phases. Spawn selection happens in execute, enters its one child using push, returns through terminal/return, and commits in the existing phase. Each activated spawn adds at most one frame, already dominated by the original 1+64*S frame bound. The catalog holds at most two definitions, each one no-input agent; selection cannot introduce recursive catalog expansion. The existing transition charging structure therefore still covers the same phase counts for these reachable 17 fixtures. Unit checks are finite evidence, not a machine-checked source theorem.

Catalog JSON matching, hashing, immutable state copies, trace storage and production manifest admission/compilation have costs outside the transition count. The catalog constructor bounds every field before constructing/serializing its small normalized manifest. No wall-clock/provider termination bound or general dynamic-manifest memory theorem follows. Root maxWork is checked after activations, so the work-824 fixture commits the leaf at 825 before unwinding the failed spawn; 825 succeeds. Inclusive nested cell work is a shared-counter difference and is not additive across wrapper records.
