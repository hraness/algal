[
  { "name": "unknown-keyword", "schema": { "type": "string", "maxLength": 2 }, "reason": "unknown schema keyword \"maxLength\"" },
  { "name": "first-unknown-keyword", "schema": { "type": "object", "zeta": 1, "additionalProperties": false }, "reason": "unknown schema keyword \"additionalProperties\"" },
  { "name": "nested-unknown-keyword", "schema": { "properties": { "a": { "type": "string", "description": "hint" } } }, "reason": "unknown schema keyword \"description\"" },
  { "name": "unknown-type", "schema": { "type": "strnig" }, "reason": "type must name a supported JSON type or a nonempty unique union" },
  { "name": "required-not-array", "schema": { "required": "owner" }, "reason": "required must list at most 64 distinct names of at most 64 UTF-16 code units" },
  { "name": "required-duplicate", "schema": { "required": ["owner", "owner"] }, "reason": "required must list at most 64 distinct names of at most 64 UTF-16 code units" },
  { "name": "required-long", "schema": { "required": ["xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"] }, "reason": "required must list at most 64 distinct names of at most 64 UTF-16 code units" },
  { "name": "properties-not-object", "schema": { "properties": [] }, "reason": "properties must map at most 64 names to schemas" },
  { "name": "properties-scalar-child", "schema": { "properties": { "owner": 1 } }, "reason": "properties must map at most 64 names to schemas" },
  { "name": "items-tuple", "schema": { "type": "array", "items": [{ "type": "string" }] }, "reason": "items must be a schema" },
  { "name": "items-default-object", "schema": { "items": { "type": "string" } }, "reason": "items requires type array" },
  { "name": "items-string-type", "schema": { "type": "string", "items": {} }, "reason": "items requires type array" },
  { "name": "enum-not-array", "schema": { "type": "string", "enum": "open" }, "reason": "enum must list 1 to 32 distinct values" },
  { "name": "enum-empty", "schema": { "type": "string", "enum": [] }, "reason": "enum must list 1 to 32 distinct values" },
  { "name": "enum-canonical-duplicate", "schema": { "type": "number", "enum": [1, 1.0] }, "reason": "enum must list 1 to 32 distinct values" },
  { "name": "enum-object-value", "schema": { "type": ["object", "string"], "enum": [{ "status": "open" }] }, "reason": "enum values must be strings, finite numbers, booleans, or null of at most 256 canonical JSON bytes" },
  { "name": "enum-long-value", "schema": { "type": "string", "enum": [""] }, "reason": "enum values must be strings, finite numbers, booleans, or null of at most 256 canonical JSON bytes" },
  { "name": "enum-type-mismatch", "schema": { "type": "string", "enum": ["open", 1] }, "reason": "enum values must match the schema type" },
  { "name": "enum-integer-mismatch", "schema": { "type": "integer", "enum": [1.5] }, "reason": "enum values must match the schema type" },
  { "name": "minimum-not-number", "schema": { "type": "number", "minimum": "0" }, "reason": "minimum must be a finite number" },
  { "name": "maximum-not-number", "schema": { "type": "number", "maximum": true }, "reason": "maximum must be a finite number" },
  { "name": "range-without-number-type", "schema": { "type": "string", "minimum": 0 }, "reason": "minimum and maximum require type number or integer" },
  { "name": "range-inverted", "schema": { "type": "number", "minimum": 5, "maximum": 1 }, "reason": "minimum exceeds maximum" },
  { "name": "nested-range-inverted", "schema": { "type": "array", "items": { "type": "integer", "minimum": 1, "maximum": 0 } }, "reason": "minimum exceeds maximum" },
  { "name": "nine-levels", "schema": { "type": "array", "items": { "type": "array", "items": { "type": "array", "items": { "type": "array", "items": { "type": "array", "items": { "type": "array", "items": { "type": "array", "items": { "type": "array", "items": { "type": "string" } } } } } } } } }, "reason": "schema exceeds 8 nested levels" }
]
